name: apply-contents-db-migrations
run-name: Apply contents-db Migrations
on:
  push:
    branches: [main]
    paths:
      - 'migrations/contents-db/**'
  workflow_dispatch:

jobs:
  migrate:
    name: Apply Cloudflare D1 Migrations
    runs-on: ubuntu-latest
    permissions:
      contents: read

    env:
      WRANGLER_SEND_METRICS: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1

      - name: Setup pnpm and Node.js
        uses: pnpm/setup@v1
        with:
          runtime: node@26.8.1
          install: false
          cache: true

      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile

      - name: Run contents-db Migrations
        uses: cloudflare/wrangler-action@v4
        with:
          command: d1 migrations apply CONTENTS_DB --remote
          workingDirectory: ./apps/web 
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
